<form method="post" action="{{ path('app_circuit_delete', {'nc': circuit.nc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ circuit.nc) }}">
    <button class="btn">Delete</button>
</form>
