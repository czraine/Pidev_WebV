<form method="post" action="{{ path('app_favplaces_delete', {'id': favplace.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favplace.id) }}">
    <button class="btn">Delete</button>
</form>
