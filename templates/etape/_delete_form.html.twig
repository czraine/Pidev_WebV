<form method="post" action="{{ path('app_etape_delete', {'rang': etape.rang}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etape.rang) }}">
    <button class="btn">Delete</button>
</form>
